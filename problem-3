# Input list
numbers = [1, 2, 8, 9, 12, 46, 76, 82, 15, 20, 30]

# Initialize result dictionary for divisors 1 through 9
result = {i: 0 for i in range(1, 10)}

# Count how many numbers are divisible by each key
for num in numbers:
    for i in result:
        if num % i == 0:
            result[i] += 1

# Print the result
print(result)
